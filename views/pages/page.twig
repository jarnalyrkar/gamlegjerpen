{% extends 'layouts/base.twig' %}

{% block content %}
<div class="container mt-5">
  <div class="row">
    <main class="col-12 col-lg-8">
      <h1>{{ post.title }}</h1>
      {{ post.content }}
    </main>
  </div>
</div>
{% endblock %}