{% extends 'layouts/base.twig' %}

{% block content %}
<main class="container mt-5">
  <div class="row">
    <div class="col-12 col-lg-10">
      {% if post.type != "sources" %}
        <h1>{{ post.title }}</h1>
      {% endif %}
      {{ post.content }}
    </div>
  </div>
</main>
{% endblock %}